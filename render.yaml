services:
  - type: web
    name: org-chart
    env: python
    buildCommand: |
      apt-get update
      apt-get install -y graphviz
      pip install -r requirements.txt
    startCommand: gunicorn app:app